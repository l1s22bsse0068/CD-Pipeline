name: wf-cd-release
on:
  push:
    branches:
      - release/**
jobs:
  build-release-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          ref: "${{github.ref}}"
      - name: Build Website With Parcel
        run: npx parcel build "./src/index.html" --dist-dir "./rel-dist" --public-url "./"
      - name: Upload Build Artifacts   
        run: actions/upload-pages-artifact@v3
        with:
          path: "./rel-dist"
          name: "release-build-artifacts"
  deploy-release-job:
    needs: "build-release-job"
    runs-on: ubuntu-latest
    environment:
      name: "release-env"
    permissions:
      id-token: write
      pages: write
    steps:
      - name: Deploy Build Artifacts in "release-env"
        uses: actions/deploy-pages@v4
        with:
          artifact_name: "release-build-artifact"
          token: "${{github.token}}"